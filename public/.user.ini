; PHP limits for admin car uploads (10–12 images, up to 5 MB each in app validation).
; Applied via public/ because Laravel document root is public/ on cPanel.
upload_max_filesize = 10M
post_max_size = 128M
max_file_uploads = 30
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
